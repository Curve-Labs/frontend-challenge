// Import the deposit components
import SwapComponent from "../components/Card";

const Swap = () => {
  return (
    <div className="App-header">
      <SwapComponent />
    </div>
  );
};

export default Swap;
